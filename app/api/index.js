const authorEndpoints       = require('./routes/author')
const novelEndpoints        = require('./routes/novel')
const characterEndpoints    = require('./routes/character')

module.exports = { characterEndpoints, novelEndpoints, authorEndpoints }
